import random

rand_num = random.randint(1, 3)
print(rand_num)

print(f"The average delay time is across multiple runs is: "
              f" {sum(self.total_delay_list) / sum(self.number_delayed_list)}")
        print(f'The standard deviation of delay time across multiple runs is: '
              f'{statistics.pstdev(self.total_delay_list)}')
